# Generated pi-server OpenAPI client

Do not hand-edit generated Kotlin sources in this directory.

With pi-server running locally, regenerate and commit this directory with:

```powershell
cd pi-companion
./scripts/generate-openapi-models.ps1
```

The Android app continues to use its hand-written client while generated models are adopted incrementally, so normal builds remain offline.
